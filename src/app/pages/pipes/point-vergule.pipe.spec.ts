import { PointVergulePipe } from './point-vergule.pipe';

describe('PointVergulePipe', () => {
  it('create an instance', () => {
    const pipe = new PointVergulePipe();
    expect(pipe).toBeTruthy();
  });
});
