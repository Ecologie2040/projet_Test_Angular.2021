import { NombreArtPipe } from './nombre-art.pipe';

describe('NombreArtPipe', () => {
  it('create an instance', () => {
    const pipe = new NombreArtPipe();
    expect(pipe).toBeTruthy();
  });
});
